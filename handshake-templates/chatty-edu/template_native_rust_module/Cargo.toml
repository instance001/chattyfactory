[package]
name = "template_native_rust_module"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.30", default-features = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
