# Anchor context (read-only once locked)

Locked: yes
Anchor: scratch

## User request
build a file sorter. needs to have an input folder and an output folder for sorted bins. sort based on file extensions.

## Model interpretation
build a file sorter. needs to have an input folder and an output folder for sorted bins. sort based on file extensions.
