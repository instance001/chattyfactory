# Anchor context (read-only once locked)

Locked: yes
Anchor: scratch

## User request
build a file sorter tool that sorts files based on file types

## Model interpretation
build a file sorter tool that sorts files based on file types
