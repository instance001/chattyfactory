Title: new project: Let's build a basic gui dashboard in python
Project: new_project_Lets_build_a_basic_gui_dashboard_in_python
Anchor: scratch

- [ ] Prepare project root | ensure_dir . | verify: dir_exists .
- [ ] Implement tkinter dashboard | write_file main.py | contents: import argparse\nimport json\nimport statistics\nimport sys\nimport tkinter as tk\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom tkinter import ttk, messagebox\n\n\n@dataclass\nclass Metric:\n    name: str\n    value: float\n    target: float\n\n    @property\n    def status(self) -> str:\n        if self.value >= self.target:\n            return "on track"\n        if self.value >= self.target * 0.8:\n            return "watch"\n        return "behind"\n\n\nDEFAULT_METRICS = [\n    Metric("Revenue", 128.0, 120.0),\n    Metric("Active users", 934.0, 1000.0),\n    Metric("Conversion", 7.4, 6.5),\n    Metric("Support tickets", 18.0, 12.0),\n]\n\n\ndef load_metrics(path: str | None) -> list[Metric]:\n    if not path:\n        return list(DEFAULT_METRICS)\n    data_path = Path(path)\n    try:\n        raw = json.loads(data_path.read_text(encoding="utf-8"))\n    except FileNotFoundError as exc:\n        raise ValueError(f"metrics file not found: {data_path}") from exc\n    except json.JSONDecodeError as exc:\n        raise ValueError(f"invalid metrics JSON: {exc}") from exc\n    if not isinstance(raw, list):\n        raise ValueError("metrics JSON must be a list of objects")\n    metrics: list[Metric] = []\n    for idx, item in enumerate(raw, start=1):\n        if not isinstance(item, dict):\n            raise ValueError(f"metric {idx} must be an object")\n        try:\n            name = str(item["name"])\n            value = float(item["value"])\n            target = float(item["target"])\n        except KeyError as exc:\n            raise ValueError(f"metric {idx} missing field: {exc.args[0]}") from exc\n        except (TypeError, ValueError) as exc:\n            raise ValueError(f"metric {idx} has non-numeric value/target") from exc\n        metrics.append(Metric(name=name, value=value, target=target))\n    if not metrics:\n        raise ValueError("metrics JSON cannot be empty")\n    return metrics\n\n\ndef summarize_metrics(metrics: list[Metric]) -> dict[str, float | int]:\n    values = [m.value for m in metrics]\n    on_track = sum(1 for m in metrics if m.status == "on track")\n    return {\n        "count": len(metrics),\n        "total": round(sum(values), 2),\n        "average": round(statistics.mean(values), 2),\n        "on_track": on_track,\n    }\n\n\nclass DashboardApp(tk.Tk):\n    def __init__(self, metrics: list[Metric]):\n        super().__init__()\n        self.title("ChattyFactory Dashboard")\n        self.geometry("760x460")\n        self.minsize(620, 360)\n        self.metrics = metrics\n        self._build_ui()\n\n    def _build_ui(self) -> None:\n        header = ttk.Frame(self, padding=16)\n        header.pack(fill="x")\n        ttk.Label(header, text="new project: Let's build a basic gui dashboard in python", font=("Segoe UI", 16, "bold")).pack(anchor="w")\n        ttk.Label(header, text="Local dashboard generated by ChattyFactory").pack(anchor="w")\n\n        summary = summarize_metrics(self.metrics)\n        cards = ttk.Frame(self, padding=(16, 0, 16, 8))\n        cards.pack(fill="x")\n        for label, value in [\n            ("Metrics", summary["count"]),\n            ("Total", summary["total"]),\n            ("Average", summary["average"]),\n            ("On track", summary["on_track"]),\n        ]:\n            card = ttk.Frame(cards, relief="ridge", padding=10)\n            card.pack(side="left", fill="x", expand=True, padx=(0, 8))\n            ttk.Label(card, text=label).pack(anchor="w")\n            ttk.Label(card, text=str(value), font=("Segoe UI", 14, "bold")).pack(anchor="w")\n\n        body = ttk.Frame(self, padding=16)\n        body.pack(fill="both", expand=True)\n        columns = ("name", "value", "target", "status")\n        tree = ttk.Treeview(body, columns=columns, show="headings", height=10)\n        for col, text in zip(columns, ["Metric", "Value", "Target", "Status"]):\n            tree.heading(col, text=text)\n            tree.column(col, anchor="center")\n        tree.column("name", anchor="w")\n        for metric in self.metrics:\n            tree.insert("", "end", values=(metric.name, metric.value, metric.target, metric.status))\n        tree.pack(fill="both", expand=True)\n\n        footer = ttk.Frame(self, padding=(16, 0, 16, 16))\n        footer.pack(fill="x")\n        ttk.Button(footer, text="Refresh summary", command=self._show_summary).pack(side="right")\n\n    def _show_summary(self) -> None:\n        summary = summarize_metrics(self.metrics)\n        messagebox.showinfo("Dashboard summary", json.dumps(summary, indent=2))\n\n\ndef run_self_test() -> None:\n    metrics = [\n        Metric("A", 10, 8),\n        Metric("B", 5, 10),\n        Metric("C", 8, 8),\n    ]\n    summary = summarize_metrics(metrics)\n    assert summary["count"] == 3\n    assert summary["total"] == 23\n    assert summary["on_track"] == 2\n    loaded = load_metrics(None)\n    assert loaded and loaded[0].name == "Revenue"\n    print("CF_OK")\n\n\ndef main(argv: list[str] | None = None) -> int:\n    parser = argparse.ArgumentParser(description="Run a small local dashboard.")\n    parser.add_argument("--data", help="Optional JSON metrics file")\n    parser.add_argument("--self-test", action="store_true", help="Run non-visual acceptance checks")\n    args = parser.parse_args(argv)\n    if args.self_test:\n        run_self_test()\n        return 0\n    try:\n        metrics = load_metrics(args.data)\n    except ValueError as exc:\n        print(f"error: {exc}", file=sys.stderr)\n        return 2\n    app = DashboardApp(metrics)\n    app.mainloop()\n    return 0\n\n\nif __name__ == "__main__":\n    raise SystemExit(main())\n | verify: contains main.py class DashboardApp
- [ ] Write project contract | write_file ProjectSpec.json | contents: {"spec_id":"chattyfactory.project_spec.v1","project_name":"new_project_Lets_build_a_basic_gui_dashboard_in_python","summary":"new project: Let's build a basic gui dashboard in python","language":"python","entrypoints":["main.py"],"expected_files":["main.py","ProjectSpec.json"],"default_commands":["py -3 main.py --self-test"],"acceptance_commands":["py -3 main.py --self-test"],"acceptance_checks":[{"command":"py -3 main.py --self-test","expect_output_contains":"CF_OK"}],"key_files":[{"path":"main.py","role":"tkinter dashboard entrypoint"},{"path":"ProjectSpec.json","role":"project contract"}],"current_features":["new project: Let's build a basic gui dashboard in python"],"invariants":["Keep the acceptance command passing after every patch.","Keep GUI state/model logic testable via --self-test."],"last_request":"new project: Let's build a basic gui dashboard in python","last_work_order_id":"","updated_at":""} | verify: contains ProjectSpec.json tkinter
- [ ] Run dashboard self-test | run py -3 main.py --self-test | verify: stdout_contains CF_OK
