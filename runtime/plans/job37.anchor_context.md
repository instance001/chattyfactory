# Anchor context (read-only once locked)

Locked: no
Anchor: scratch

## User request
let's patch the gpu monitor to track real gpu telemetry.

## Model interpretation
let's patch the gpu monitor to track real gpu telemetry.
