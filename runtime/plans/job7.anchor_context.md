# Anchor context (read-only once locked)

Locked: yes
Anchor: scratch

## User request
build a file sorting tool. input folder for files to be added for sorting. output folder for sorted bins. sort files based on type.

## Model interpretation
build a file sorting tool. input folder for files to be added for sorting. output folder for sorted bins. sort files based on type.
